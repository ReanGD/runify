import 'package:flutter/material.dart';

class Settings {
  final metricsEnabled = true;
  final grpcAddress = "/tmp/runify.socket";
  final windowSize = const Size(150, 200);
  final windowOffset = const Offset(0, 0);

  Settings();
}
